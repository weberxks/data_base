
Задание 6

	1. start transaction
	   insert into 
		sample.users () 
	   select * from shop.users where id=1;
 	   commit;

	2. create or REPLACE 
	        view v1 (name,id)
	   as select 
		products.name, 
		catalogs.name 
	   from 
		products, 
		catalogs 
	   where 
		catalogs.id = products.catalog_id;
